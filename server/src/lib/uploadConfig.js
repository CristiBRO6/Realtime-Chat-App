// FILE UPLOAD CONFIGURATION
const MAX_FILE_SIZE = 10 * 1024 * 1024; //10MB
const ACCEPTED_FILE_TYPES = 'image/png,image/gif,image/jpeg,image/jpg';

module.exports = { MAX_FILE_SIZE, ACCEPTED_FILE_TYPES };